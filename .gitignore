# Upstream hackathon materials — cloned separately, not part of this project
2026-ntu-scaling-for-quantum-advantage-and-beyond/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Environments / editors
.env
.vscode/
.idea/

unrelated_reference/
docs/
artifacts/
legacy/
logs/


# results_drl/ sweep output: keep the JSON data (reproducibility) and the
# aggregate curve, ignore the regeneratable per-seed binaries (checkpoints,
# per-seed plots, solved GIFs) -- rerun run_experiment.py to recreate them.
